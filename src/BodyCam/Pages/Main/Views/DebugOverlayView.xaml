<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BodyCam.ViewModels"
             x:Class="BodyCam.Pages.Main.Views.DebugOverlayView"
             x:DataType="vm:MainViewModel">

    <Grid IsVisible="{Binding DebugVisible}"
          VerticalOptions="End"
          MaximumHeightRequest="250"
          BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceTranslucent}, Dark={StaticResource SurfaceTranslucentDark}}">
        <VerticalStackLayout Spacing="4" Padding="8">
            <!-- Phase 6.1: AEC metrics overlay -->
            <Label x:Name="AecDebugLabel"
                   Text="{Binding AecDebugText}"
                   FontFamily="Consolas" FontSize="10"
                   BackgroundColor="{StaticResource Overlay}" TextColor="Lime"
                   Padding="6,2"
                   IsVisible="{Binding DebugVisible}" />

            <Frame Padding="8" CornerRadius="8">
                <ScrollView AutomationId="DebugScroll">
                    <Label AutomationId="DebugLabel"
                           Text="{Binding DebugLog}"
                           FontSize="11"
                           FontFamily="OpenSansRegular"
                           TextColor="Gray" />
                </ScrollView>
            </Frame>
        </VerticalStackLayout>
    </Grid>
</ContentView>
